"use strict"; // it is used to treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser so this line of code will not give any type of pop-up but in browser it will give.

console.log(3 
    +
     3) // code readability should be high, the code should be easily readable 

console.log("Hitesh")


let name = "Om"             //string datatype
let age = 18                //integer datatype
let isLoggedIn = false      //boolean datatype
let state;                  //undefined, it can be assigned any values

/*  number => 2 to power 53
    bigint
    string => ""
    boolean => true/false
    null => standalone value
    undefined => 
    symbol => unique
*/


// object
console.log(typeof name); //string
console.log(typeof undefined); // undefined
console.log(typeof null); // object